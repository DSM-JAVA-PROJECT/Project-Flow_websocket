package com.projectflow.projectflowwebsocket.domain.message.payload;

public enum MessageType {
    MESSAGE,
    JOIN,
    READ,
    CREATE,
    CREATE_PLAN,
    JOIN_PLAN,
    DELETE_PLAN,
    REMOVE,
    EXIT
}